"""Integration tests for MorphDB Python SDK.

These tests require a running MorphDB server.
Start the test server with:
    docker compose -f docker-compose.test.yml up -d

Run tests with:
    pytest tests/integration/ -v
"""
